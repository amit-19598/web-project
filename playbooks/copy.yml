---
- name: Copy file to remote server
  hosts: all
  become: yes

  tasks:
    - name: Copy data directory to remote
      copy:
        src: data/
        dest: /tmp/data/
        owner: root
        group: root
        mode: '0755'
