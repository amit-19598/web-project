<<<<<<< HEAD
- name: Install Apache httpd on RHEL 10 (skip GPG)
  hosts: webservers
  become: true

  tasks:
    - name: Update package cache
      dnf:
        update_cache: yes
        skip_broken: yes

    - name: Install httpd
      dnf:
        name: httpd
        state: present
        validate_certs: yes
        installroot: /
        disable_gpg_check: yes

    - name: Start and enable httpd
      service:
        name: httpd
        state: started
        enabled: true
=======
---
- name: Create a directory on remote servers
  hosts: all
  become: yes   # Run with sudo privileges
  tasks:
    - name: Create a directory
      ansible.builtin.file:
        path: /opt/myapp
        state: directory
        owner: root
        group: root
        mode: '0755'
>>>>>>> 2e24493 (test)
